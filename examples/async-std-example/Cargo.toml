[package]
name = "async-std-example"
version = "0.1.0"
edition = "2021"
publish = false
description = "Async-std runtime example for ranked-semaphore"

[[bin]]
name = "basic"
path = "src/main.rs"

[dependencies]
ranked-semaphore = { workspace = true }
async-std = { workspace = true }

[package]
name = "smol-example"
version = "0.1.0"
edition = "2021"
publish = false
description = "Smol runtime example for ranked-semaphore"

[[bin]]
name = "basic"
path = "src/main.rs"

[dependencies]
ranked-semaphore = { workspace = true }
smol = { workspace = true }
